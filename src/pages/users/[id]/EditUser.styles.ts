export const editUserStyles = {
/*
 * @property {string} container - Applies a maximum width of extra-large (max-w-xl) to limit content width,
 * centers the container horizontally using automatic margins (mx-auto), and adds consistent padding (p-6)
 * around the content for visual spacing.
 */
  container: "max-w-xl mx-auto p-6",
 
/* @property {string} title - Styles the page title text with a larger font size (text-2xl), bold weight (font-bold),
 * and a bottom margin (mb-4) to create spacing between the title and the next element.
 */
  title: "text-2xl font-bold mb-4",

/* @property {string} notFoundContainer - Provides padding (p-6) around the fallback content displayed
 * when a user is not found, ensuring the layout remains consistent with the main content.
 */
  notFoundContainer: "p-6",
}